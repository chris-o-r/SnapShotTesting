version: "3.8"

services:
  snap-shot-api:
    build:
      context: ./snap-shot-api
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    networks:
      - app-network

  frontend:
    build:
      context: ./snap-shot-testing-fe
      dockerfile: Dockerfile
    ports:
      - "3000:80"
    networks:
      - app-network

networks:
  app-network:
    driver: bridge
